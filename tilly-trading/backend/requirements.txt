# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# websockets is pulled by uvicorn[standard]; left unpinned so it can satisfy
# metaapi-cloud-sdk's (<12) constraint.

# ---- Data / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Database (SQLAlchemy 2.0 + async + migrations) ----
sqlalchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
greenlet==3.1.1
psycopg2-binary==2.9.10
alembic==1.14.0

# ---- Auth / security ----
pyjwt[crypto]==2.10.1
bcrypt==4.2.1
email-validator==2.2.0

# ---- Cache / queue ----
redis==5.2.1
celery==5.4.0

# ---- Broker integration ---- (29.x uses modern deps with py3.11 wheels)
metaapi-cloud-sdk==29.1.1

# ---- HTTP client ----
httpx==0.28.1

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.0
